# Smart Energy Optimizer — Pro (APIs + ML + Optimization)

Production-quality Smart Energy Optimizer:
- Streamlit UI
- External data support (OpenWeather, optional EIA)
- ML forecasting (RandomForest / XGBoost optional)
- Integer programming optimization (PuLP)
- SQLite audit log, tests, Dockerfile

## Quick start
1. Copy `.env.example` to `.env` and add OPENWEATHER_API_KEY if available.
2. Install:
